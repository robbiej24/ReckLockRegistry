apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "recklock.fullname" . }}-config
  labels:
    {{- include "recklock.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.env }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
