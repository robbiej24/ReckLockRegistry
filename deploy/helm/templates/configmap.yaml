apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "agenttrust.fullname" . }}-config
  labels:
    {{- include "agenttrust.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.env }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
