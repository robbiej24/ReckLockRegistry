apiVersion: v1
kind: Service
metadata:
  name: {{ include "agenttrust.fullname" . }}
  labels:
    {{- include "agenttrust.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "agenttrust.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
