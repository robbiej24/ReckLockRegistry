apiVersion: v1
kind: Service
metadata:
  name: {{ include "recklock.fullname" . }}
  labels:
    {{- include "recklock.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "recklock.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
