{{- if .Values.database.url }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "recklock.fullname" . }}-db
  labels:
    {{- include "recklock.labels" . | nindent 4 }}
type: Opaque
stringData:
  database-url: {{ .Values.database.url | quote }}
{{- end }}
